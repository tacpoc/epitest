This folder contains config transforms common for all build configurations.

These transforms are applied before any build-specific config transforms.

Make sure all files in this folder have their Build Action set to "None" 
to avoid this folder being included when performing a Publish.