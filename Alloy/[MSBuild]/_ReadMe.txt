This folder contains files used by MSBuild as part of building/deploying the website.

MSBuild scripts are executed as post-build events.

Make sure all files in this folder have their Build Action set to "None" 
to avoid this folder being included when performing a Publish.